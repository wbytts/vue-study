<template>
  <div>
    <!--
      $root
      $children
      $parent
      $refs

      $options
      $el
      $data
      $attrs
      $props
      $listeners
      $scopedSlots
      $slots

      $vnode
      $createElement

      $route
      $router

      $store

      $isServer
      $ssrContext

      _c
      _data
      _directInactive
      _events
      _hasHookEvent
      _inactive
      _isBeingDestroyed
      _isDestroyed
      _isMounted
      _isVue
      _renderProxy
      _routerRoot
      _self
      _staticTrees
      _uid
      _vnode
      _watcher
      _watchers

    -->
  </div>
</template>

<script>
export const name = 'vue-learn# 组件实例# 实例属性浏览';

export default {
  mounted() {
    console.log(this);
  },
};
</script>

<style>
</style>
